{
  "name": "intelligent-bistro-mobile-first",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix mobile",
    "backend": "npm run dev --prefix backend",
    "mobile": "npm run start --prefix mobile"
  }
}
